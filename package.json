{
  "name": "cronos",
  "version": "0.1.0",
  "private": true,
  "description": "Cronos - Orquestrador em Node + React com TypeScript",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"server,realtime,web\" -c \"blue,magenta,green\" \"npm --silent --workspace=@cronos/server run dev\" \"npm --silent --workspace=@cronos/realtime run dev\" \"npm --silent --workspace=@cronos/web run dev\"",
    "dev:server": "npm --workspace=@cronos/server run dev",
    "dev:realtime": "npm --workspace=@cronos/realtime run dev",
    "dev:web": "npm --workspace=@cronos/web run dev",
    "build": "npm run build:shared && npm run build:server && npm run build:realtime && npm run build:web",
    "build:shared": "npm --workspace=@cronos/shared run build",
    "build:server": "npm --workspace=@cronos/server run build",
    "build:realtime": "npm --workspace=@cronos/realtime run build",
    "build:web": "npm --workspace=@cronos/web run build",
    "start": "npm --workspace=@cronos/server run start",
    "start:realtime": "npm --workspace=@cronos/realtime run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "rollup": "npm:@rollup/wasm-node@^4.63.3",
    "esbuild": "npm:esbuild-wasm@^0.25.0"
  }
}
